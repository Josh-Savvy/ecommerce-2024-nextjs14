import type Product from "@/interfaces/product.interface";
import React from "react";

const RelatedProducts = (prod: Product) => {
	return <div>RelatedProducts</div>;
};

export default RelatedProducts;
